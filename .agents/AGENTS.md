# Workspace Rules

- **Publishing Protocol**: Absolutely NEVER execute the publishing scripts (e.g. `publish.sh`, `flutter pub publish`) or officially publish the packages without receiving EXPLICIT confirmation and approval from the user. Always present the final state and wait for the user to say "publish" or similar before proceeding.
