name: hyper_render_devtools_ui
description: "DevTools panel UI for HyperRender — tree inspector, style viewer, layout debug."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

flutter:
  uses-material-design: true
